import { PropsWithChildren } from "react"
import "./RestContainer.css"

export default function RestContainer({children, } : PropsWithChildren) {

    return (
        <div>LIST OF RESTUARNANT</div>
    )
}